name: 'custom GITHUB_ENV var Demo'

on:  workflow_dispatch

jobs:
  custom_GITHUB_ENV:
    runs-on: windows-latest
    steps:
    - name: Use the value
      id: step_two
      shell: cmd
      run: |
         echo " manikName=${{ GITHUB_WORKSPACE}}"
         echo " manikName1=${{env.GITHUB_WORKSPACE}}"
         $manikName1
         $manikName


        
